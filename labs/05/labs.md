Given a words.txt files containing one word per line, write a program to allow
user to specify an input file, and print out every word that is a typo.

Better yet, make some recommendation for auto-correct.